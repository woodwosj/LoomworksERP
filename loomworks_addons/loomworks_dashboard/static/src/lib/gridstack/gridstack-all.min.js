/**
 * GridStack.js v10.3.1 - Stub
 *
 * This is a placeholder for the GridStack.js library.
 * Download the production build from: https://github.com/gridstack/gridstack.js
 * Version required: >= 10.x
 *
 * To install:
 *   1. Download gridstack-all.js from the GridStack releases page
 *   2. Replace this file with the minified production build
 *
 * GridStack is used for the drag-and-drop dashboard widget layout.
 */
(function() {
    'use strict';
    if (typeof window.GridStack !== 'undefined') {
        return; // Already loaded
    }
    console.warn(
        'Loomworks Dashboard: GridStack.js library not installed. ' +
        'Drag-drop layout will not be available. ' +
        'See loomworks_dashboard/static/src/lib/gridstack/gridstack-all.min.js for instructions.'
    );
    // Provide a minimal no-op stub so the module can load without errors
    window.GridStack = {
        init: function(opts, el) {
            console.warn('GridStack.init() called but library is not installed.');
            return {
                on: function() { return this; },
                off: function() { return this; },
                addWidget: function() { return null; },
                removeWidget: function() {},
                removeAll: function() {},
                update: function() {},
                save: function() { return []; },
                load: function() {},
                destroy: function() {},
                enableMove: function() { return this; },
                enableResize: function() { return this; },
                compact: function() {},
                el: el || document.createElement('div'),
            };
        },
    };
})();
